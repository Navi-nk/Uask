<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@drawable/side_nav_bar"
    android:gravity="top|center"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    android:weightSum="1">

    <!-- http://stackoverflow.com/questions/33290120/navigation-drawer-header-->

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        app:srcCompat="@drawable/userprofile"
        android:layout_above="@+id/tv2"
        android:layout_centerHorizontal="true" />

    <ToggleButton
        android:id="@+id/account_view_icon_button"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="center"
        android:alpha=".54"
        android:background="@drawable/toggle_expand_collapse"
        android:textOff="@null"
        android:textOn="@null"
        android:layout_marginEnd="10dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true" />

    <TextView
        android:id="@+id/tv2"
        android:layout_width="wrap_content"
        android:paddingTop="20dp"
        android:layout_height="40dp"
        android:text="Faculty"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:layout_alignBaseline="@+id/account_view_icon_button"
        android:layout_alignBottom="@+id/account_view_icon_button"
        android:layout_alignStart="@+id/tv1" />

    <TextView
        android:id="@+id/tv1"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:paddingTop="20dp"
        android:text="Name"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:layout_marginStart="10dp"
        android:layout_alignParentStart="true"
        android:layout_above="@+id/account_view_icon_button" />
</RelativeLayout>
